version: '3.3'

services:
    web:
        build: .
        container_name: lightbot-frontend
        volumes:
            - ./docker/certs:/etc/nginx/certs
        restart: always
        ports:
            - "80:80"
            - "443:443"
        networks:
            - frontend

networks:
    frontend:
        external: true
